# CNN-specific overrides
model:
  type: "cnn"
  filters: [32, 64, 128]
  kernel_size: 3
  pool_size: 2
  dropout: 0.3
  learning_rate: 0.001

training:
  epochs: 150
  batch_size: 16

data:
  reshape: [32, 32, 1]  # Reshape for CNN input

experiment:
  name: "cnn_experiment"